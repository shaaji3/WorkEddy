server {
    listen 80;
    server_name _;

    root  /var/www/html/public;
    index index.php;

    # Allow video uploads up to 210 MB (matches PHP post_max_size)
    client_max_body_size 210M;

    # All requests routed through the single PHP entry point
    location / {
        try_files $uri /index.php?$query_string;
    }

    # Block access to sensitive files
    location ~ /\.(env|git|htaccess) {
        deny all;
    }

    location ~ \.php$ {
        include        fastcgi_params;
        fastcgi_param  SCRIPT_FILENAME /var/www/html/public/index.php;
        fastcgi_pass   api:9000;
        fastcgi_param  REQUEST_URI $request_uri;
    }

    # Serve uploaded videos from shared storage
    location /storage/videos/ {
        alias /storage/uploads/videos/;
        add_header Accept-Ranges bytes;
        add_header Cache-Control "private, max-age=3600";
    }

    # Static assets served directly
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}